// import { Injectable } from '@angular/core';
// import { NativeStorage } from '@ionic-native/native-storage';

// @Injectable()

// export class StorageProvider {

//     constructor(
//         private nativeStorage: NativeStorage,
//     ) { }

//     async getData(key: string) {
//         let value: any;
//         await this.nativeStorage.getItem(key).then(res => value = res);

//         return value;
//     }

//     async isExist(key: string) {
//         let value: boolean;
//         await this.nativeStorage.getItem(key).then(res => res ? (value = true) : (value = false));

//         return value;
//     }
// }
